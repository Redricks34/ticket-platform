# Ticket Platform

Полнофункциональная платформа техподдержки с системой тикетов, построенная на современных технологиях.

## 🏗 Архитектура проекта

Проект построен с использованием микросервисной архитектуры:

- **Backend API** - FastAPI сервис для управления тикетами
- **Frontend** - (планируется) веб-интерфейс для пользователей
- **Database** - MongoDB для хранения данных
- **Cache/Notifications** - Redis для уведомлений в реальном времени

## 📁 Структура проекта

```
ticket-platform/
├── backend/                    # Backend API сервис
│   ├── main.py                # Основной файл приложения
│   ├── models.py              # Модели данных (Pydantic)
│   ├── database.py            # Подключение к MongoDB
│   ├── services.py            # Бизнес-логика
│   ├── routes.py              # API маршруты
│   ├── notifications.py       # Сервис уведомлений
│   ├── config.py             # Конфигурация приложения
│   ├── requirements.txt      # Зависимости Python
│   ├── Dockerfile            # Docker образ API
│   └── docker-compose.yml    # Docker Compose конфигурация
├── frontend/                   # Frontend приложение (планируется)
├── .env                       # Переменные окружения
├── .gitignore                 # Git ignore файл
└── README.md                  # Документация проекта
```

## 🚀 Возможности системы

### Backend API:
- ✅ Создание, обновление и удаление тикетов
- ✅ Система комментариев к тикетам  
- ✅ Фильтрация и поиск тикетов
- ✅ Пагинация результатов
- ✅ Уведомления в реальном времени (WebSocket)
- ✅ Интеграция с Redis для уведомлений
- ✅ MongoDB для хранения данных
- ✅ Контейнеризация с Docker
- ✅ Статистика по тикетам

### Frontend:
- ✅ Современный веб-интерфейс для пользователей
- ✅ Просмотр своих тикетов (4 в ряду, адаптивно)
- ✅ Создание новых тикетов с валидацией
- ✅ Фильтрация по статусу, категории, поиск
- ✅ Статистика и счетчики тикетов
- ✅ Модальные окна с деталями тикетов
- ✅ Уведомления в реальном времени
- ✅ Адаптивный дизайн для всех устройств

## Быстрый старт

### 1. Запуск с Docker Compose (рекомендуется)

```bash
# Клонировать репозиторий
git clone <repository-url>
cd ticket-platform/backend

# Запустить все сервисы
docker-compose up -d
```

### 2. Локальная установка Backend

```bash
# Перейти в папку backend
cd ticket-platform/backend

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить MongoDB и Redis локально
# MongoDB: https://www.mongodb.com/docs/manual/installation/
# Redis: https://redis.io/docs/getting-started/installation/

# Запустить приложение
python main.py
```

## API Endpoints

### Тикеты

- `POST /tickets/` - Создать тикет
- `GET /tickets/` - Получить список тикетов (с фильтрами)
- `GET /tickets/{id}` - Получить тикет по ID
- `PATCH /tickets/{id}` - Обновить тикет
- `DELETE /tickets/{id}` - Удалить тикет
- `POST /tickets/{id}/comments` - Добавить комментарий

### Статистика

- `GET /tickets/stats/summary` - Общая статистика по тикетам

### Уведомления

- `WebSocket /tickets/ws/notifications` - Подключение для уведомлений

### Системные

- `GET /` - Проверка работоспособности
- `GET /health` - Health check

## Фильтрация тикетов

API поддерживает следующие фильтры:

- `status` - Статус тикета (открыт, в_процессе, закрыт, решен)
- `priority` - Приоритет (низкий, средний, высокий, критический)
- `category` - Категория (техническая, биллинг, общий, баг_репорт, запрос_функции)
- `assignee_id` - ID исполнителя
- `reporter_email` - Email автора тикета
- `created_from` / `created_to` - Диапазон дат создания
- `search_text` - Текстовый поиск по заголовку и описанию
- `page` / `page_size` - Пагинация

### Пример запроса

```bash
curl "http://localhost:8000/tickets/?status=открыт&priority=высокий&page=1&page_size=10"
```

## Модели данных

### Тикет

```json
{
  "title": "Проблема с авторизацией",
  "description": "Пользователи не могут войти в систему",
  "status": "открыт",
  "priority": "высокий",
  "category": "техническая",
  "reporter_email": "user@example.com",
  "reporter_name": "Иван Иванов",
  "assignee_id": "60f1b2b3c9e77c001f5e4a1b",
  "assignee_name": "Петр Петров",
  "comments": [...],
  "created_at": "2025-12-04T10:00:00Z",
  "updated_at": "2025-12-04T10:30:00Z"
}
```

### Комментарий

```json
{
  "author_name": "Техподдержка",
  "author_email": "support@example.com",
  "content": "Проблема исправлена в новой версии",
  "created_at": "2025-12-04T11:00:00Z"
}
```

## WebSocket уведомления

Подключитесь к `ws://localhost:8000/tickets/ws/notifications` для получения уведомлений в реальном времени:

```javascript
const ws = new WebSocket('ws://localhost:8000/tickets/ws/notifications');

ws.onmessage = function(event) {
    const notification = JSON.parse(event.data);
    console.log('Новое уведомление:', notification);
};
```

Типы событий:
- `created` - Тикет создан
- `updated` - Тикет обновлен
- `comment_added` - Добавлен комментарий

## Конфигурация

Настройки в файле `.env`:

```env
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=ticket_platform
REDIS_URL=redis://localhost:6379
SECRET_KEY=your-secret-key
```

## Разработка

### Запуск в режиме разработки

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Доступ к документации

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Управление базой данных

Mongo Express доступен по адресу: http://localhost:8081
- Логин: `admin`
- Пароль: `pass`

## Производство

### Безопасность

1. Измените `SECRET_KEY` в production
2. Настройте правильные CORS origins
3. Используйте HTTPS
4. Настройте аутентификацию/авторизацию

### Масштабирование

1. Используйте несколько экземпляров API
2. Настройте MongoDB replica set
3. Добавьте Redis Cluster для высокой доступности
4. Используйте load balancer (nginx, HAProxy)

## Мониторинг

- Health check endpoint: `/health`
- Логи приложения через стандартный logging Python
- Метрики MongoDB и Redis через их встроенные инструменты

## 🛠 Быстрый старт проекта

### Запуск всего проекта

```bash
# Клонировать репозиторий и перейти в корневую папку
git clone <repository-url>
cd ticket-platform

# Запустить все сервисы через Docker Compose
docker-compose up -d
```

### Запуск только Backend

```bash
# Перейти в папку backend
cd backend

# Запустить backend сервисы
docker-compose up -d
```

## 📊 Адреса сервисов

После запуска доступны:
- **🎨 Frontend**: http://localhost:3000 - Веб-интерфейс пользователей
- **🔧 Backend API**: http://localhost:8000 - REST API
- **📚 API Документация**: http://localhost:8000/docs - Swagger UI
- **📖 ReDoc**: http://localhost:8000/redoc - Альтернативная документация
- **🗄️ Mongo Express**: http://localhost:8081 (admin/pass) - Управление БД

## 🎯 Что дальше?

1. **Запустите проект**: `docker-compose up -d`
2. **Откройте веб-интерфейс**: http://localhost:3000
3. **Создайте первый тикет** через веб-интерфейс
4. **Проверьте API документацию**: http://localhost:8000/docs
5. **Наслаждайтесь уведомлениями** в реальном времени!

## 🏆 Результат

Вы получили:
- ✅ **Полнофункциональный Backend API** с FastAPI
- ✅ **Современный Frontend** с веб-интерфейсом
- ✅ **MongoDB** для хранения данных  
- ✅ **Redis** для уведомлений в реальном времени
- ✅ **WebSocket** для мгновенных обновлений
- ✅ **Docker** контейнеризацию всей системы
- ✅ **Масштабируемую архитектуру**
- ✅ **Готовую к использованию систему техподдержки**

Платформа готова к работе и дальнейшему развитию!

## Лицензия

MIT License