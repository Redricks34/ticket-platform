# Руководство по развертыванию системы поддержки

## Быстрый запуск

### 1. Запуск всех сервисов
```bash
docker-compose up -d
```

### 2. Проверка статуса
```bash
docker-compose ps
```
Все контейнеры должны быть в состоянии "Up".

### 3. Доступ к сервисам
- **Веб-интерфейс**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **Документация API**: http://localhost:8000/docs
- **Админка БД**: http://localhost:8081
- **Проверка здоровья**: http://localhost:8000/health

## Тестирование системы

### Проверка работоспособности API
```powershell
# Проверка здоровья системы
Invoke-RestMethod -Uri http://localhost:8000/health

# Получение списка тикетов
Invoke-RestMethod -Uri http://localhost:8000/tickets/
```

### Создание тестового тикета
```powershell
$body = @{
    title = "Тестовый тикет"
    description = "Проверка работы системы"
    category = "техническая"
    reporter_email = "test@example.com"
    reporter_name = "Тестовый пользователь"
} | ConvertTo-Json

Invoke-RestMethod -Uri http://localhost:8000/tickets/ -Method POST -Body $body -ContentType "application/json"
```

## Структура системы

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   Backend API   │    │   MongoDB       │
│   (Port 3000)   │◄──►│   (Port 8000)   │◄──►│   (Port 27017)  │
│                 │    │                 │    │                 │
│ • Веб-интерфейс │    │ • REST API      │    │ • База данных   │
│ • Тикеты 4×4    │    │ • WebSocket     │    │ • Документы     │
│ • Формы         │    │ • Валидация     │    │ • Индексы       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                │
                                ▼
                       ┌─────────────────┐
                       │     Redis       │
                       │   (Port 6379)   │
                       │                 │
                       │ • Уведомления   │
                       │ • Кэширование   │
                       │ • WebSocket     │
                       └─────────────────┘
```

## Решение проблем

### Контейнер не запускается
```bash
# Просмотр логов
docker-compose logs [service-name]

# Перезапуск сервиса
docker-compose restart [service-name]
```

### Очистка и перезапуск
```bash
# Остановка и удаление контейнеров
docker-compose down

# Полная очистка (включая volumes)
docker-compose down -v

# Пересборка и запуск
docker-compose up -d --build
```

## Статус развертывания ✅

- ✅ Backend API запущен и работает
- ✅ MongoDB подключена
- ✅ Redis подключен
- ✅ Frontend интерфейс доступен
- ✅ WebSocket уведомления функционируют
- ✅ Создание тикетов работает
- ✅ Комментарии к тикетам работают
- ✅ Все контейнеры запущены успешно

## Созданные тестовые данные

В системе уже созданы 5 тестовых тикетов различных категорий:
1. Test ticket (общий)
2. Проблема с входом (техническая)
3. Вопрос по биллингу (биллинг)
4. Баг в интерфейсе (баг_репорт)
5. Запрос новой функции (запрос_функции)

Система полностью готова к использованию! 🎉